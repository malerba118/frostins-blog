import { HStack } from "@chakra-ui/react";
import InlineCode from "../components/InlineCode";
import FileAction from "../components/FileAction";
import FilePath from "../components/FilePath";
import CodeBlock from "../components/CodeBlock";

# Reorder a list using framer-motion

Now let's add a new file <FileAction action="add" path="/foo.js" />.
Now that <FilePath path="/foo.js" /> exists, we can go ahead and
delete it <FileAction action="delete" path="/foo.js" />.

## Section Two

Now this should be easier `const foo = 1;`

<CodeBlock code="const foo = 1;" path="/App.js" />

Now let's add a new file <FileAction action="add" path="/foo.js" />.
Now that <FilePath path="/foo.js" /> exists, we can go ahead and
delete it <FileAction action="delete" path="/foo.js" />.

## Section Two

Now this should be easier `const foo = 1;`

```jsx path="/App.js" showLineNumbers
const bar = <div>bar</div>;
```

<HStack>
  <FileAction action="update" path="/foo.js" code="const foo = 1;"label="foo = 1;" />

{" "}
<FileAction
  action="update"
  path="/foo.js"
  code="const foo = 2;"
  label="foo = 2;"
/>

  <FileAction action="update" path="/foo.js" code="const foo = 3;" label="foo = 3;" />
</HStack>
